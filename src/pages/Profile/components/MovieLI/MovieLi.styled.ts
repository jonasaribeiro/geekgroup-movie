import styled from 'styled-components';

export const StyledLi = styled.li`
    display: flex;
    flex-direction: column;
    padding: 16px 0 20px;
    position: relative;
    align-items: center;
`;

export const Button1 = styled.button`
    background-image: linear-gradient(to right, rgb(190 46 0), rgb(255 52 0));
    color: white;
    font-size: 20px;
    position: absolute;
    top: 0;
    margin: auto;
    padding: 5px 10px;
    border-radius: 10px;
`;

export const Button2 = styled.button`
    background-image: linear-gradient(rgb(190 107 0), rgb(255 52 0));
    color: white;
    font-size: 20px;
    position: absolute;
    bottom: 0;
    margin: auto;
    padding: 5px 10px;
    border-radius: 10px;
`;

export const ImgPoster = styled.img``;
